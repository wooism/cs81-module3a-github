# cs81-module3a-github
Assignment 3A for CS 81 at Santa Monica College

## Functions in journal.js

greet(name) - returns a greeting with a person's name

speak() - writes "good day" to the console

personTime(name,time) - writes a greeting with a name and time to the console

calcArea(radius) - returns the area of a circle when given a radius

printNames(names) - loops through an array and logs each name to the console